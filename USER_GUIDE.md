# dGC-JP 操作ガイド

## はじめに

dGC-JP（Digital Growth Chart for Japan）は、日本人小児の成長データを入力し、
成長曲線、身長・体重SDS、肥満度、身長速度（Height Velocity: HV）などを
確認するための臨床補助ツールです。

通常の使用に、プログラミングやJSONなどのコンピュータに関する専門知識は必要ありません。

基本的には、

1. 出生情報を入力する
2. 測定日・身長・体重を入力する
3. 成長曲線と評価結果を確認する

という流れで使用できます。

> **注意**
>
> dGC-JPは教育・研究・臨床補助を目的としたツールです。
> 確定診断を目的とした医療機器ではありません。
> 表示された結果は、原データや患者の臨床情報とあわせて、
> 最終的に医師等の専門家が判断してください。

---

# 1. 基本的な使い方

## 1-1. 出生情報を入力する

最初に児の基本情報を入力します。

主な入力項目は以下です。

- 性別
- 生年月日
- 在胎週数
- 在胎日数
- 管理ID

早産児で修正年齢を使用する場合は、在胎週数・在胎日数を正確に入力してください。

在胎日数は **0～6日の整数** で入力します。

---

## 1-2. 測定値を入力する

各測定時点について、以下を入力します。

- 測定日
- 身長（cm）
- 体重（kg）

複数回の測定値を入力すると、経時的な成長を成長曲線上で確認できます。

身長速度（HV）を評価する場合は、複数時点の身長測定が必要です。

---

# 2. 成長曲線を見る

入力した測定値は、日本人小児の成長基準に基づく成長曲線上に表示されます。

成長曲線では、児の身長・体重が年齢とともにどのように推移しているかを
視覚的に確認できます。

単回のSDSだけでなく、

- 成長曲線に沿って推移しているか
- SDSが時間とともに変化していないか
- 成長速度に変化がないか

など、経時的な変化もあわせて確認してください。

---

# 3. 評価結果を見る

入力されたデータから、利用可能な範囲で以下の指標が表示されます。

## 身長SDS

同年齢・同性の日本人小児の身長分布に対して、
児の身長がどの位置にあるかを標準偏差スコア（SDS）で示します。

例：

- 0 SDS：基準の中央付近
- -2 SDS：基準より低い側
- +2 SDS：基準より高い側

SDSは診断そのものではありません。
年齢、成長速度、家族歴、基礎疾患などとあわせて評価してください。

---

## 体重SDS

年齢・性別に応じた日本人小児の体重基準に対する位置をSDSで表示します。

---

## 肥満度

身長別標準体重または年齢別標準体重を用いた肥満度を表示します。

表示される値の解釈は、年齢や臨床状況を含めて総合的に行ってください。

---

# 4. 修正年齢について

早産児では、暦年齢（実際の生年月日から計算した年齢）に加えて、
修正年齢を用いた評価が参考になる場合があります。

dGC-JPでは、37週未満で出生した児について、
入力された在胎週数・在胎日数をもとに修正年齢を計算します。

## dGC-JPでの取り扱い

現在のdGC-JPでは、早産児について、
40週0日相当日以降、かつ**暦年齢3.0歳まで（3.0歳を含む）**、
修正年齢が算出可能な場合に、
修正年齢を身長・体重SDSの計算および成長曲線上の修正年齢表示に使用します。

40週0日相当日より前の測定では修正年齢を適用せず、
身長・体重SDSは暦年齢を用いて計算します。

3.0歳を超えた場合は、これらの評価に修正年齢による補正を行いません。

一方、肥満度、Raw HV、Suwa HVおよびHV-SDSは、
現在の実装では暦年齢を用いて計算します。

これはdGC-JPにおける計算上の取り扱いです。
個々の患者における修正年齢の臨床的な使用については、
患者背景や診療目的に応じて判断してください。

## 在胎日数について

在胎日数には **0～6** の整数を入力してください。

例：

- 32週0日 → 在胎週数「32」、在胎日数「0」
- 35週4日 → 在胎週数「35」、在胎日数「4」

在胎日数が不正な場合、dGC-JPはその値を自動的に「0日」とみなして
計算を続行することはありません。

入力内容を確認して修正してください。

---

# 5. 身長速度（Height Velocity: HV）について

身長速度（HV）は、一定期間にどれだけ身長が伸びたかを
1年間あたりの伸び（cm/年）として表したものです。

dGC-JPでは、目的の異なる2種類のHVを区別して表示します。

## Raw HV

前回の測定値と今回の測定値から計算した身長速度です。

直近の身長変化を確認するための値です。

測定間隔が短すぎる場合は算出されません。

## Suwa HV

Suwaらによる身長速度基準との比較に使用するため、
おおむね1年間隔となる測定値の組み合わせを使用して算出します。

dGC-JPでは、1年間隔に近い測定値を選択するため、
**0.95～1.05年** をアプリケーション上の許容範囲として使用しています。

この0.95～1.05年という範囲は、
Suwaらの原著で規定された閾値ではなく、
dGC-JPにおける測定値選択のためのアプリケーション仕様です。

条件を満たす過去の測定値がない場合は、
Raw HVが表示されてもSuwa HVやHV-SDSが表示されないことがあります。

## HV-SDS

条件を満たすSuwa HVについては、
年齢・基準データに基づいてHV-SDSが表示されます。

Raw HVとSuwa HVは目的と計算に使用する測定間隔が異なるため、
同じものとして扱わないでください。

---

# 6. 「データ保存」と「データ読込」

dGC-JPに入力したデータは、ブラウザを再読み込みすると消去されます。

あとで同じデータを続けて使用したい場合は、
画面の **「データ保存」** を使用してください。

保存すると、入力内容が **JSON形式のファイル** として
使用中のコンピュータに保存されます。

---

# 7. JSONファイルとは？

## JSONを知らなくても使用できます

「JSON（ジェイソン）」は、
コンピュータが情報を整理して保存するためのファイル形式の名前です。

dGC-JPでは、

- 管理ID
- 生年月日
- 性別
- 在胎週数・在胎日数
- 測定日
- 身長
- 体重

などの情報をひとまとまりにして保存するために使用します。

**JSONについて勉強したり、ファイルの中身を直接編集したりする必要はありません。**

通常は、

> dGC-JPで「データ保存」  
> ↓  
> JSONファイルを保管  
> ↓  
> 次回dGC-JPで「データ読込」

という使い方だけで十分です。

---

## ExcelやCSVとは何が違うのですか？

CSVは、Excelなどで扱いやすい「表形式」のデータ保存に適した形式です。

JSONは、

- 児の基本情報
- 出生情報
- 複数回の測定値

のような、互いに関連する情報を
ひとまとまりとして保存することに適しています。

dGC-JPでは、入力した状態をまとめて保存・復元する目的で
JSON形式を使用しています。

---

## JSONファイルを直接編集してもよいですか？

通常はおすすめしません。

JSONファイルは一定の形式で記録されているため、
文字や記号を誤って変更すると、dGC-JPで読み込めなくなることがあります。

内容を修正したい場合は、

1. dGC-JPでファイルを読み込む
2. 画面上で入力内容を修正する
3. あらためて「データ保存」する

という方法を推奨します。

---

# 8. 保存したデータを再び使用する

以前保存したデータを使用するときは、
dGC-JPのデータ読込機能から保存済みのJSONファイルを選択します。

正常に読み込まれると、出生情報や測定データが復元されます。

JSONの形式や必須項目など、読み込みを継続できない問題がある場合は、
読み込みエラーが表示されます。

一方、基準範囲外の測定値など一部の注意事項は、
ファイルの読み込み自体を妨げないため、
読み込み時に画面上のエラーが表示されない場合があります。

読み込み後は、復元された出生情報や測定値が正しいことを確認してください。

その場合は、

- dGC-JPから保存したJSONファイルか
- ファイルを手作業で変更していないか
- 別のファイルを誤って選択していないか

を確認してください。

---

# 9. データの取り扱いについて

dGC-JPに入力されたデータは、現在の実装ではブラウザ内で処理され、
dGC-JPの外部サーバーへ患者データを送信・保存する仕組みにはなっていません。

一方、「データ保存」を行うと、
JSONファイルそのものは使用中のコンピュータに保存されます。

このファイルには入力した情報が含まれます。

そのため、保存したJSONファイルは、
通常の診療情報と同様に所属施設の情報管理・個人情報保護のルールに従って
適切に取り扱ってください。

特に、

- 私物端末への保存
- メールへの添付
- クラウドストレージへの保存
- 他者とのファイル共有

を行う場合は、所属施設の規定を確認してください。

---

# 10. 入力時に警告やエラーが表示された場合

入力画面では、確認が必要な一部の入力条件について警告やエラーを表示します。

例：

- 測定日が生年月日より前になっている
- 測定時の年齢が18歳を超えている
- 在胎週数が22週未満または44週以上である
- 在胎日数が0～6の整数ではない

dGC-JPは、すべての入力値について臨床的な妥当性を自動的に判定するものではありません。
警告やエラーが表示されないことは、入力値が正しいことや臨床的に正常であることを示すものではありません。

警告の有無にかかわらず、計算結果だけで判断せず、
出生情報および測定値の原資料を確認してください。

---

# 11. 「算出不可」と表示される場合

すべての測定値について、すべての指標が必ず算出されるわけではありません。

例えば、

- 必要な身長・体重が入力されていない
- 対応する基準値がない
- 身長速度を計算するための測定間隔が不足している
- Suwa HVの評価条件を満たす過去の測定値がない
- 入力値が計算条件を満たしていない

場合などには、一部の結果が「算出不可」または非表示になることがあります。

値が表示されないこと自体を異常所見として解釈せず、
入力データと計算条件を確認してください。

---

# 12. 印刷・画像として保存する

dGC-JPでは、成長曲線を診療記録や検討用資料として扱いやすいように、
印刷用表示や画像出力機能を備えています。

画像として保存する場合はPNG形式で出力できます。

印刷・保存したデータについても、
患者情報が含まれる場合は所属施設の情報管理ルールに従って取り扱ってください。

---

# 13. よくある質問

## Q. JSONという言葉を初めて聞きました。使えますか？

はい。

JSONの知識は必要ありません。

「データ保存」で保存し、
次回「データ読込」でそのファイルを選択するだけで使用できます。

---

## Q. JSONファイルはExcelで開く必要がありますか？

ありません。

dGC-JPで保存・読込するためのファイルとして、そのまま使用してください。

---

## Q. ブラウザを閉じてもデータは残りますか？

dGC-JPでは入力中のデータを恒久保存することを前提としていません。

特にブラウザを再読み込みすると入力内容は消去されます。

必要なデータは、作業を終了する前に「データ保存」で保存してください。

---

## Q. Raw HVは表示されていますが、Suwa HVが表示されません。

Raw HVとSuwa HVでは、使用する測定値の条件が異なります。

Suwa HVの評価に適した、おおむね1年間隔の過去の測定値が存在しない場合、
Raw HVだけが表示されることがあります。

---

## Q. 修正年齢が表示されません。

以下を確認してください。

- 在胎週数・在胎日数が正しく入力されているか
- 在胎日数が0～6の整数になっているか
- 児が修正年齢の適用対象となっているか
- 暦年齢がdGC-JPの補正適用範囲内か

---

## Q. 数値が表示されたので、そのまま診断に使用できますか？

いいえ。

dGC-JPは臨床判断を補助するためのツールです。

SDS、肥満度、HVなどの値は、
患者の病歴、診察所見、家族歴、検査所見、過去の成長経過などと
あわせて評価してください。

---

# 14. 困ったときの確認順序

結果がおかしい、または期待した値が表示されない場合は、
次の順に確認してください。

1. 生年月日
2. 性別
3. 在胎週数・在胎日数
4. 測定日
5. 身長・体重の入力値と単位
6. 過去の測定値
7. 画面に表示されている警告・エラー

特に、転記ミス、日付の誤り、cmとm、kgとgなどの単位間違いがないかを
最初に確認してください。

---

# 15. このガイドについて

本ガイドは、dGC-JPを日常診療・教育・研究で使用する臨床医・医療従事者向けの
操作説明です。

詳細な計算方法、使用している基準データ、参考文献については、
README.mdおよびDATA_SOURCES.mdを参照してください。